function solve(input) {
    let firstNumber = parseInt(input[0]);
    let lastNumber = parseInt(input[input.length -1]);


    console.log(firstNumber + lastNumber);
    
}

solve(['10', '17', '22', '33']);