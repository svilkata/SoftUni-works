function printPersonInfo(firstName, secondName, age) {
    let personInfo = {
        firstName: firstName,
        lastName: secondName,
        age: age
    };

    return personInfo;

}

printPersonInfo("Peter",
    "Pan",
    "20"
);